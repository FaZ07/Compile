# COMPILE — environment

# Groq (intent parser + synthesizer brain)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Anakin Wire (optional — when set, swaps mocked travel sources for live ones)
WIRE_API_KEY=
WIRE_BASE_URL=https://anakin.io/v1/wire
